This is a simple kernel for two architectures, aarch64 and amd64 (x86_64). Both kernels are written in different languages (aarch64 in rust and amd64 in C) and both are in different development phases.
Amd64 being more advanced. 
